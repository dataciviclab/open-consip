select
  {year}::INTEGER as anno_riferimento,
  normalize_string("Comparto") as comparto,
  normalize_string("Tipologia_Amministrazione") as tipologia_amministrazione,
  normalize_string("Denominazione_amministrazione") as denominazione_amministrazione,
  normalize_string("Denominazione_gara") as denominazione_gara,
  normalize_string("Stato_gara") as stato_gara,
  normalize_string("Denominazione_lotto") as denominazione_lotto,
  normalize_string("Stato_lotto") as stato_lotto,
  normalize_string("Identificativo_lotto") as identificativo_lotto,
  normalize_string("Categoria_merceologica") as categoria_merceologica,
  normalize_string("Tipo_procedura") as tipo_procedura,
  normalize_string("Oggetto_appalto") as oggetto_appalto,
  normalize_string("Criterio_aggiudicazione") as criterio_aggiudicazione,
  normalize_string("Regione_PA") as regione_pa,
  normalize_string("Provincia_PA") as provincia_pa,
  normalize_string("Sigla_prov_PA") as sigla_prov_pa,
  try_cast("Data_pubblicazione" as DATE) as data_pubblicazione,
  try_cast("Data_aggiudicazione" as DATE) as data_aggiudicazione,
  normalize_italian_integer("Base_asta") as base_asta,
  normalize_italian_integer("Valore_aggiudicato") as valore_aggiudicato,
  cast_bigint("Durata_contratto_presunta") as durata_contratto_presunta,
  cast_bigint("Durata_contratto_stipula") as durata_contratto_stipula,
  cast_bigint("Numero_fornitori_partecipanti") as numero_fornitori_partecipanti,
  try_cast("Latitudine_PA" as DOUBLE) as latitudine_pa,
  try_cast("Longitudine_PA" as DOUBLE) as longitudine_pa
from raw_input
